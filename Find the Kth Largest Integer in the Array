class Solution:
    def kthLargestNumber(self, nums: List[str], k: int) -> str:
        nums2 = [int(n) for n in nums]
        nums2.sort()
        print(nums)
        return str(nums2[len(nums2)-k])
